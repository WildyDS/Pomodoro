// @flow

import {blue, red, green, black, white} from './Colors'

const Colors = {
  main: blue,
  accent: red,
  fill: green,
  background: white
}

const Fonts = {
  normal: {
    fontSize: 12,
    color: black
  }
}

export default {
  Colors, Fonts
}
